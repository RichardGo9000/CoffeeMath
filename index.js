import CoffeeMath from './js/class/App.js';  //import app class

const app = new CoffeeMath();  //instantiate new instance of coffeemath
// To turn on logging add true to the fn below: I.e. `await app.init(true);`
await app.init();  //init app with asyncronous javascript

